pyyaml>=6.0
requests>=2.31
trendspy>=0.1                # google_trends adapter; soft-skipped if missing
sentence-transformers>=2.7   # stage_cluster embeddings (pulls in torch ~500MB)
scikit-learn>=1.3            # provides sklearn.cluster.HDBSCAN
numpy>=1.26

# LLM backend: the `claude` CLI (Claude Code) — install from
# https://claude.com/claude-code. The anthropic SDK is NOT required;
# llm.py shells out to `claude -p` for every LLM call so the pipeline
# runs on a Claude Code subscription instead of a paid API key.
